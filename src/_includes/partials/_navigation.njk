{% set currentUrl = page.url %}
{% set content = collections["content_" + lang] %}

<nav>
  <ul class="tabs">
    {%- for item in content %}
      <li class="tab">
        <a
          {% if currentUrl === item.url %} aria-current="page" {% endif %}
          href="{{ item.url }}">
            {{ item.data.title }}
        </a>
      </li>
    {%- endfor %}
  </ul>
</nav>
