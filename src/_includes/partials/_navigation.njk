{% set currentUrl = page.url %}

<nav>
  <ul class="tabs">
    {%- for item in collections.page %}
      <li class="tab">
        <a
          {% if currentUrl === item.url %} aria-current="page" {% endif %}
          href="{{ item.url }}">
            {{ item.data.title }}
        </a>
      </li>
    {%- endfor %}
  </ul>
</nav>
