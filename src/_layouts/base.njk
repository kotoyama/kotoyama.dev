<!DOCTYPE html>
<html lang="{{ page.lang }}" data-theme="light">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="{{ meta[page.lang].description }}">
    <title>{{ meta[page.lang].title }}</title>
    <link rel="stylesheet" href="/css/index.css">
  </head>
  <body>
    <div id="app">
      <div class="terminal">
        {% include "partials/_toolbar.njk" %}
        {% include "partials/_navigation.njk" %}
        <main class="window">{{content | safe}}</main>
      </div>
    </div>
  </body>
</html>
